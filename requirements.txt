# Runtime / deployment dependencies only.
# Development and test tooling lives in requirements-dev.txt.

gradio>=6.13.0  # >=6.6.0 for SSRF fix, >=6.7.0 for path traversal fix on Windows/Python 3.13+; >=6.9.0 to skip malformed audioop-lts marker in 6.8.0; >=6.12.0 to avoid unconditional audioop-lts conflict; >=6.13.0 for Python 3.13 compatibility
Markdown>=3.4,<4.0
bleach>=6.1.0
curl-cffi>=0.7.0  # >=0.7.0 required; libcurl vulnerability in <=0.6.4
requests>=2.32.2
websockets>=13.0
plotly>=5.0.0
numpy>=1.26.0
pandas>=1.5.0
dataclasses-json>=0.5.7
yfinance>=0.2.51
fastapi==0.133.0  # >=0.133 allows starlette>=1.0.1 (CVE-2026-48710); was 0.127 with starlette<0.51
uvicorn[standard]>=0.24.0
pydantic==2.12.5  # pinned for FastAPI compatibility
h11==0.16.0  # pinned for compatibility with uvicorn/httpx

# YAML / configuration dependencies
PyYAML==6.0.3  # updated for Python 3.12 compatibility
ruamel.yaml==0.18.17  # updated for Python 3.12 compatibility

# Serverless / database dependencies
mangum==0.17.0  # pinned for consistency with pyproject.toml
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0

# MCP (Model Context Protocol) dependencies
mcp>=1.23.0  # >=1.9.4 for DoS fix, >=1.23.0 for DNS rebinding protection fix
fastmcp>=0.1.0

# Auth / rate-limiting dependencies
PyJWT>=2.13.0
passlib[bcrypt]>=1.7.4
slowapi>=0.1.4

# Observability dependencies
prometheus_client>=0.19.0
structlog>=24.1.0

# Security override pins (not directly imported; pinned to satisfy vulnerability advisories)
urllib3>=2.6.3  # pinned by Snyk advisory; transitive dep of requests/httpx
zipp>=3.19.1  # pinned by Snyk advisory; transitive dep of several packages
starlette>=1.3.1  # CVE-2026-48710 / Safety 99546; allowed by fastapi==0.133.0

version: 2.1

executors:
  dummy-executor:
    docker:
      - image: cimg/base:stable

commands:
  dummy-step:
    steps:
      - run: echo "CI migrated to GitHub Actions. This is a dummy job to satisfy legacy branch protection requirements."

jobs:
  python-lint:
    executor: dummy-executor
    steps: [dummy-step]
  python-test:
    executor: dummy-executor
    steps: [dummy-step]
  python-security:
    executor: dummy-executor
    steps: [dummy-step]
  frontend-lint:
    executor: dummy-executor
    steps: [dummy-step]
  frontend-build:
    executor: dummy-executor
    steps: [dummy-step]
  docker-build:
    executor: dummy-executor
    steps: [dummy-step]

workflows:
  build-and-test:
    jobs:
      - python-lint
      - python-test
      - python-security
      - frontend-lint
      - frontend-build
      - docker-build

# Frontend
FRONTEND_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:8000

# Backend
ALLOWED_ORIGINS=http://localhost:3000
BACKEND_PORT=8000
ENV=development
GRADIO_SERVER_PORT=7860

# Auth/application DB (local). Run `python -m scripts.migrate_database` before application startup.
# ADMIN_PASSWORD is consumed only by that explicit operator command; remove it from the runtime environment.
# Prefer sqlite:dev.db — avoid sqlite:///./… under this repo's resolver.
ADMIN_PASSWORD=replace-with-a-strong-password
ADMIN_USERNAME=admin
DATABASE_URL=sqlite:dev.db
SECRET_KEY=replace-with-a-long-random-secret-at-least-32-chars

# Hosted / durable boundaries (staging & production). Leave unset for local SQLite-only demos.
# ASSET_GRAPH_DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/graph
# COORDINATION_DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/coordination
# POSTGRES_URL=postgresql://USER:PASSWORD@HOST:5432/app
# PostgreSQL operator only: path to a mode-0600, operator-attested target binding.
# Never commit the referenced document or bake it into an image.
# FARDB_POSTGRES_TARGET_BINDINGS_FILE=/run/secrets/fardb.target-bindings.json
#
# ADR 0007 / H-P0-04: untrusted provider DB roles for scripts/check_database_authorization.py.
# Leave unset to use defaults (anon,authenticated). If set empty in GitHub Actions, workflows unset it.
# FARDB_UNTRUSTED_DATABASE_ROLES=anon,authenticated
# Comma-separated exposed schemas for the authz gate (unset => public only). Use the FULL inventoried list
# (include public when exposed). In GitHub Actions these must be Environment secrets, not variables.
# FARDB_EXPOSED_DATABASE_SCHEMAS=public
# Optional per-boundary overrides when inventories differ by URL:
# FARDB_EXPOSED_DATABASE_SCHEMAS_DATABASE=public
# FARDB_EXPOSED_DATABASE_SCHEMAS_ASSET_GRAPH=public,graph_api
# FARDB_EXPOSED_DATABASE_SCHEMAS_COORDINATION=public
# FARDB_EXPOSED_DATABASE_SCHEMAS_POSTGRES=public

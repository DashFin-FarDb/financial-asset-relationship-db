name: Greetings

"on":
  pull_request_target:
  issues:

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - uses: actions/first-interaction@1c4688942c71f71d4f5502a26ea67c331730fa4d # v3.1.0
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        issue_message: '👋 Thanks for opening your first issue! A maintainer will review it soon.'
        pr_message: '🎉 Thanks for your first pull request! Our CI checks will run automatically.'

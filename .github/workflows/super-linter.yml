# This workflow executes several linters on changed files based on languages used in your code base whenever
# you push a code or open a pull request.
#
# You can adjust the behavior by modifying this file.
# For more information, see:
# https://github.com/super-linter/super-linter
name: Lint Code Base

"on":
  push:
    branches: ["main"]
    paths-ignore:
      - "**/*.md"
      - "docs/**"
      - "**/*.png"
      - "**/*.jpg"
  pull_request:
    types: [opened, synchronize, reopened]
    branches: ["main"]
    paths-ignore:
      - "**/*.md"
      - "docs/**"
      - "**/*.png"
      - "**/*.jpg"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
jobs:
  run-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Full git history is needed to get a proper list of changed files within `super-linter`
          fetch-depth: 0
          persist-credentials: false

      - name: Lint Code Base
        uses: super-linter/super-linter@4ce20838b8ab83717e78138c5b3a1407148e0918 # v8.7.0
        env:
          VALIDATE_ALL_CODEBASE: false
          DEFAULT_BRANCH: "main"
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Keep frontend linting/formatting with the repository's dedicated
          # ESLint and Prettier lanes, including Biome validators added in v8.
          VALIDATE_BIOME_FORMAT: false
          VALIDATE_BIOME_LINT: false
          VALIDATE_JAVASCRIPT_ES: false
          VALIDATE_JSX: false
          VALIDATE_TYPESCRIPT_ES: false
          VALIDATE_TSX: false
          VALIDATE_JSCPD: false
          # Tracking cleanup/re-enablement for the validator below:
          # https://github.com/DashFin-FarDb/financial-asset-relationship-db/issues/1496
          # NOTE: VALIDATE_GITHUB_ACTIONS and VALIDATE_CHECKOV are intentionally
          # omitted so they remain enabled by default in Super-Linter v8.
          GITHUB_ACTIONS_CONFIG_FILE: "actionlint.yaml"
          # YAML_PRETTIER currently flags broad legacy workflow formatting debt.
          # Keep dedicated workflow semantics checks elsewhere until #1496.
          VALIDATE_YAML_PRETTIER: false
          VALIDATE_PYTHON_BLACK: false
          VALIDATE_PYTHON_FLAKE8: false
          VALIDATE_PYTHON_ISORT: false
          VALIDATE_PYTHON_MYPY: false
          VALIDATE_PYTHON_PYLINT: false
          # Keep Python formatting out of super-linter; black/ruff/pre-commit own it.
          VALIDATE_PYTHON_RUFF_FORMAT: false
